# Phase 1: 설정 관리자 & LLM 라우터
pyyaml>=6.0

# LLM API 클라이언트 (사용하는 에이전트에 맞게 설치)
anthropic>=0.40.0          # Claude API
google-generativeai>=0.8.0 # Gemini API
openai>=1.50.0             # OpenAI API

# Phase 2: PDF 전처리 & 청구항 파서
pypdfium2>=4.0.0           # Chrome PDFium 엔진, 한국어 포함 모든 PDF 강건 처리
opendataloader-pdf>=2.4.3  # Java CLI 래퍼, 레이아웃 인식 마크다운 (폴백용)

# Phase 3: 외부 DB 검색
python-dotenv>=1.0.0          # .env 파일 로드 (search_pipeline.py)
python-epo-ops-client>=3.1.0  # EPO OPS API (토큰 갱신·쓰로틀링 자동 처리)
patent-client>=4.4.0          # USPTO(PatentsView·PEDS)·Google Patents 통합 클라이언트

# Phase 4: 로컬 RAG 파이프라인
sentence-transformers>=5.5.0
qdrant-client>=1.7.0
langchain-text-splitters>=0.3.0

# Phase 5: 매칭 + 할루시네이션 검증 + 출력 (추가 의존성 없음)

# Web Dashboard Interface
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
